#! /bin/csh

cd $PROJ_DIR/mgen/params

running "TsTcp2Fmq -params TsTcp2Fmq.sixnet"
if ($status == 1) then
  TsTcp2Fmq -params TsTcp2Fmq.sixnet |& \
    LogFilter -d $ERRORS_LOG_DIR -p TsTcp2Fmq -i sixnet &
endif

